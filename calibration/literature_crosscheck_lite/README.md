# Literature cross-check (lite)

Tracked snapshot of `per_star_bias_rms.csv` from `python -m validation.compare_literature_rvs` (step 08 lite).

Full report (gitignored): `validation_output/literature_crosscheck_lite/`.
Regenerate with the playbook recipe in `docs/validation_playbook.md`.
