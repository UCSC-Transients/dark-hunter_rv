# Literature cross-check (full)

Tracked snapshots from `python -m validation.compare_literature_rvs` with `--summaries-glob` (step 08-full).

- `per_star_bias_rms.csv` — pipeline − literature bias/RMS
- `external_per_star_bias_rms.csv` — LAMOST/RAVE − literature (when present)

Full report (gitignored): `validation_output/literature_crosscheck_full/`.

**Coverage note:** unique El-Badry stars with both APF diagnostics and literature epochs on disk may be <10; see `REPORT.md` Acceptance section.
